<form method="post" action="{{ path('request_service_delete', {'id': request_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ request_service.id) }}">
    <button class="btn">Delete</button>
</form>
